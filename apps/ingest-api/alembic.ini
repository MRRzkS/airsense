[alembic]
script_location = migrations
prepend_sys_path = src
timezone = UTC

# The database URL is not set here. env.py reads it from Settings so that
# migrations and the application can never disagree about which database
# they are pointed at.
