"""Entities, value objects and rules.

This package imports nothing from `application`, `infrastructure` or `api`, and
nothing from any third-party framework — not even Pydantic. It is plain stdlib
so the rules can be read, reasoned about and tested without a running system.

Enforced by the import-linter contracts in pyproject.toml, not by convention.
"""
